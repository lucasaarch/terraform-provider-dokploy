---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dokploy Provider"
description: |-
  The Dokploy provider manages resources on a Dokploy instance.
---

# dokploy Provider

The Dokploy provider manages resources on a Dokploy instance.

## Example Usage

```terraform
terraform {
  required_providers {
    dokploy = {
      source = "lucasaarch/dokploy"
    }
  }
}

provider "dokploy" {
  endpoint = "https://dokploy.example.com"
  # api_key is read from the DOKPLOY_API_KEY environment variable.
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Dokploy API key sent as the `x-api-key` header. May also be set via the `DOKPLOY_API_KEY` environment variable.
- `endpoint` (String) Base URL of the Dokploy instance, e.g. `https://dokploy.example.com`. May also be set via the `DOKPLOY_ENDPOINT` environment variable.
